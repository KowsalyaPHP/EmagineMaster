import { RockClientDirective } from './rock-client.directive';

describe('RockClientDirective', () => {
  it('should create an instance', () => {
    const directive = new RockClientDirective();
    expect(directive).toBeTruthy();
  });
});
